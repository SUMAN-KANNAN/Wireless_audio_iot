# Core Flask and WebSocket dependencies
Flask==2.3.3
Flask-Sock==0.7.0
itsdangerous==2.1.2
Jinja2==3.1.2
MarkupSafe==2.1.3
Werkzeug==2.3.7
websockets==12.0 # For virtual_esp32.py client

# For robust, production-grade state management
redis==5.0.1

# For audio processing on the server
# Using stable, widely supported versions to improve compatibility.
numpy==1.26.4
scipy==1.13.1

# Production deployment (optional for development)
gunicorn>=20.0
gevent-websocket>=0.10.1

# For mDNS service discovery (makes local development easier)
zeroconf==0.131.0